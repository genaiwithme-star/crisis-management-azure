# Twitter Worker

This worker triggers the Twitter API every 2 minutes.

## Setup

1. Copy `.env.example` to `.env` and add your Twitter Bearer Token.
2. Install dependencies:
```bash
npm install
```
3. Run the worker:
```bash
npm start
```